import 'package:flutter/material.dart';

class ManageIMG01 extends StatelessWidget {
  const ManageIMG01({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome to the ManageIMG01'),
      ),
    );
  }
}
